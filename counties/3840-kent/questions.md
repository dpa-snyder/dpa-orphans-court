# Questions — 3840 Kent County

- Which Adults table should be treated as authoritative: `Adults` or `Copy of Adults`? Do we need a reconciliation step?
- Should the \"KC deleted folders\" list be used to suppress imports, rename records, or only as a QA reference?
- Is `Puzzle pieces.xlsx` in scope for migration cleanup or just internal QA notes?
- Where should the KC container list come from (separate export/source for barcode/location lookups)?
- Do you want a KC-specific column mapping in the script or a pre-rename step to match the NCC headers?
