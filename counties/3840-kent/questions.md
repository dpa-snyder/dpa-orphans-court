# Questions - 3840 Kent County

- Which Adults table should be treated as authoritative: `Adults` or `Copy of Adults`?
- Pinned: Should we merge the KC oversize barcode list into Adults (by last/first name match) for Barcode field population?
