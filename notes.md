# Orphans Notes

# KC:
## of rows is by child, 
adult info is carried over from adult table
where the Record ID matches both tables
append "adult" to the new headers in the child table

# NCC:
## of rows is by child
adult info carried over
where records ID matches both tables
append "adult" to the new headers in the child table

# SC:
## of rows is by child
adult info carried over
where records ID matches both tables
append "adult" to the new headers in the child table

Blanks for names: nobody cares


## Barcode/Holdings Info
3 queries 
based on the Contents column
carry over data from Barcode and ShelfBarcode

NCC - 431
KC - 211
SC - 289


## For Oversized Data:
where LastName, FirstName, MiddleName and Oversized is "Yes" = true
append OS Barcode columns to from Oversized Column 